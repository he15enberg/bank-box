name: bank_box
description: Bank SMS reader and Excel exporter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  shadcn_flutter: 0.0.47
  flutter_sms_inbox: ^1.0.2
  permission_handler: ^12.0.1
  syncfusion_flutter_xlsio: ^27.1.58
  path_provider: ^2.1.4
  share_plus: ^10.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
